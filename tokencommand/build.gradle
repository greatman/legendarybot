dependencies {
    compile project(':api')
}

jar {
    manifest {
        attributes ('Plugin-Class' : 'com.greatmancode.legendarybot.commands.token.TokenCommand',
                'Plugin-Id' : 'tokenCommand',
                'Plugin-Provider' : 'Greatman',
                'Plugin-Version' : '0.0.1')

    }
}